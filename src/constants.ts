// Highest windows 10 currently supports (May 2019 update)
export const LATEST_SUPPORTED_EMOJI_VERSION = '12.0';

export const SUPPORTED_OS = ['WIND'];
export const UNSUPPORTED_SYMBOL = '🪙';

// Windoes does not support flag emojis (amazing...)
export const EXCLUDED_SUBGROUPS: string[] = ['country-flag', 'subdivision-flag'];
