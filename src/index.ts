// const world = '🗺️';

// export function hello(word: string = world): string {
//     return `Hello ${world}! `;
// }

import { loadEmojiList } from './utils/getEmojiList';

const value = loadEmojiList();
//console.log(value);
